name: Deploy

on:
  push:
    branches: [main]

jobs:
  lint-and-build:
    name: Lint & Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build
        env:
          DATABASE_URL: postgresql://dummy:dummy@localhost:5432/dummy
          BOARD_API_SECRET: ci-secret

  agent-check:
    name: Agent (Python)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        working-directory: agent
        run: uv sync --frozen --no-dev

      - name: Syntax check
        working-directory: agent
        run: uv run python -m py_compile main.py bot.py parser.py api_client.py

  deploy:
    name: Deploy to Dokploy
    runs-on: ubuntu-latest
    needs: [lint-and-build, agent-check]

    steps:
      - name: Trigger Dokploy deploy
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            -X POST "${{ secrets.DOKPLOY_WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -H "X-GitHub-Event: push" \
            -d '{"ref":"refs/heads/main"}' \
            | grep -q "^2" && echo "Deploy triggered" || (echo "Deploy trigger failed" && exit 1)
